# Dockerized Flask App on AWS EC2

A Python Flask web app containerized with Docker and deployed on AWS EC2.

## Tech Stack
- Python + Flask
- Docker + DockerHub
- AWS EC2

## Run locally
docker pull prashiii/my-docker-app:v1
docker run -p 5000:5000 prashiii/my-docker-app:v1
